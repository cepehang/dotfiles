[user]
  name = {{ .username }}
  email = {{ .email }}
  signingkey = ~/.ssh/id_ed25519.pub

[gpg]
  format = ssh

[fetch]
  prune = true

[pull]
  rebase = true

[core]
  sshCommand = ssh -i ~/.ssh/id_ed25519
